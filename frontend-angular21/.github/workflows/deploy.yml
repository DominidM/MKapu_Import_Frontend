name: Deploy Frontend

on:
  push:
    branches:
      - produccion

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: TU_IP_DEL_SERVIDOR
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            sudo /root/deploy_front.sh